<form method="post" action="{{ path('app_business_type_delete', {'id': business_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ business_type.id) }}">
    <button class="btn">Delete</button>
</form>
