<form method="post" action="{{ path('app_consumer_delete', {'id': consumer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ consumer.id) }}">
    <button class="btn">Delete</button>
</form>
